pipeline {
  agent any

  tools {
    nodejs '11.1.0'
  }
  
  stages {
    stage('Deploy') {
      steps {
        script {
          withCredentials([string(credentialsId: 'NOW_TOKEN', variable: 'NOW_TOKEN')]) {
            // Aquí ejecutas tu comando usando ${env.NOW_TOKEN}
          sh "now --token ${env.NOW_TOKEN} --yes"

          }
        }
      }
    }
  }
} 